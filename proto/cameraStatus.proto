syntax = "proto3";

package mmind;

message CameraStatus
{
    string ip = 1;
    string version = 2;
    repeated string serialId = 3;
    string eyeId = 4;
    int32 cameraType = 5;  // 0: S3, 1: P-series cameras
    string commit = 6;
}
